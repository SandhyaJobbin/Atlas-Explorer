---
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app/src/lib/exploration-trail.ts
  - app/src/features/training/MapExplorerPage.tsx
autonomous: true
gap_closure: false
---

# Plan 01-03: Waypoint Marker Positioning Verification

**Objective:** Ensure map waypoint/pointer markers appear within visible map bounds after the viewBox fix, and verify trail positioning still works correctly.

<tasks>

<task id="1" type="execute">
<name>Verify exploration trail positioning</name>
<read_first>
- app/src/lib/exploration-trail.ts
- app/public/maps/north-america.svg
- app/src/components/map/InteractiveMap.tsx
</read_first>
<action>
Review `exploration-trail.ts` to confirm the trail positioning logic uses `getBBox()` correctly. The trail uses `bbox.x + bbox.width / 2` and `bbox.y + bbox.height / 2` to compute centroids. After the viewBox change, these SVG-coordinate calculations should still work because getBBox() returns coordinates in the SVG's internal coordinate space (not screen space). Verify the transform `translate(${lastPoint.x}px, ${lastPoint.y}px)` applies correctly. No code changes expected — this is a verification task. If the trail appears misaligned after the viewBox fix, adjust the coordinate transformation to account for the new viewBox origin.
</action>
<acceptance_criteria>
- Trail markers appear at the center of clicked regions
- Trail path draws correctly within the SVG bounds
- No trail markers appearing outside the visible map area
</acceptance_criteria>
</task>

<task id="2" type="execute">
<name>Verify fly-to-panel animation positioning</name>
<read_first>
- app/src/features/training/MapExplorerPage.tsx
- app/src/components/map/InteractiveMap.tsx
</read_first>
<action>
Review the fly-to-panel animation code in `MapExplorerPage.tsx` (lines 434-463). It uses `RegionFlightSource.rect` (from `getBoundingClientRect()`) for absolute screen positioning and `RegionFlightSource.bbox` (from `getBBox()`) for the SVG viewBox on the flying clone. After the viewBox change, verify the animation still positions correctly. The `getBoundingClientRect()` returns screen coordinates (unaffected by viewBox), while `getBBox()` returns SVG-internal coordinates (also unaffected by viewBox). If the animation appears misaligned, adjust the coordinate mapping between screen and SVG space.
</action>
<acceptance_criteria>
- Fly-to animation positions the flying clone at the correct region
- Animation works on all 4 map screens
- No visual glitch or misalignment during flight
</acceptance_criteria>
</task>

<task id="3" type="execute">
<name>Visual verification across all map screens</name>
<read_first>
- app/src/features/training/MapExplorerPage.tsx
- app/src/features/games/PinRush.tsx
- app/src/features/games/ReviewRound.tsx
- app/src/features/results/ResultsPage.tsx
</read_first>
<action>
Manually verify (or write a visual test) that waypoint/pointer markers appear within visible map bounds on all 4 map use sites:
1. MapExplorerPage (Discover North America) — trail markers and hover popups
2. PinRush (Pin It game) — target region indicators
3. ReviewRound — region markers during review
4. ResultsPage — heatmap overlay markers

All markers should be visible within the SVG viewport after the viewBox expansion. No markers should be clipped or positioned outside the visible area.
</action>
<acceptance_criteria>
- All markers visible on MapExplorerPage
- All markers visible on PinRush
- All markers visible on ReviewRound
- All markers visible on ResultsPage heatmap
- No markers clipped at map edges
</acceptance_criteria>
</task>

</tasks>

<verification>
- Click 5+ regions on MapExplorer — trail markers should appear at region centers
- Start Pin It game — target indicator should be visible within map bounds
- Open Results page — heatmap markers should align with regions
- All markers visible at 100% zoom and at various browser zoom levels
</verification>

<must_haves>
- Trail markers positioned correctly after viewBox fix
- Fly-to animation works on all screens
- All markers visible within map bounds on all 4 map use sites
</must_haves>
