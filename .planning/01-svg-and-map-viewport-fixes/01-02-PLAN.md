---
wave: 1
depends_on: []
files_modified:
  - app/src/lib/session.ts
  - app/src/components/map/InteractiveMap.tsx
  - app/src/features/training/MapExplorerPage.tsx
  - app/src/features/training/TrainingCompletePage.tsx
  - app/src/features/results/ExpeditionReport.tsx
  - app/src/__tests__/session.test.ts
autonomous: true
gap_closure: false
---

# Plan 01-02: DC Reconciliation and Region Count Fix

**Objective:** Fix the 3-way mismatch between SVG (64 paths), states.json (63 entries), and TOTAL_REGIONS (64) so training completes at exactly 63 regions.

<tasks>

<task id="1" type="execute">
<name>Set TOTAL_REGIONS to 63</name>
<read_first>
- app/src/lib/session.ts
- app/public/data/states.json
</read_first>
<action>
In `app/src/lib/session.ts` line 12, change `const TOTAL_REGIONS = 64;` to `const TOTAL_REGIONS = 63;`. This matches the 63 entries in states.json (50 US states + 13 Canadian provinces/territories).
</action>
<acceptance_criteria>
- `TOTAL_REGIONS` equals 63 in session.ts
- Training completion check `session.training.mapExplorerClicked.length >= TOTAL_REGIONS` now triggers at 63 clicks
</acceptance_criteria>
</task>

<task id="2" type="execute">
<name>Exclude DC from SVG listener registration</name>
<read_first>
- app/src/components/map/InteractiveMap.tsx
- app/public/maps/north-america.svg
</read_first>
<action>
In `InteractiveMap.tsx` Effect A (around line 192), where listeners are registered on `.atlas-region` elements, add a filter to exclude elements with `data-code="DC"`. The selector or filter should skip DC paths so clicking DC does not increment the region counter. Example: change `querySelectorAll('.atlas-region')` to filter out elements where `el.dataset.code === 'DC'`.
</action>
<acceptance_criteria>
- Clicking DC on the map does NOT add it to `mapExplorerClicked` array
- DC path still renders visually (not removed from SVG)
- All other 63 regions still register click listeners correctly
</acceptance_criteria>
</task>

<task id="3" type="execute">
<name>Replace hardcoded 64 in MapExplorerPage</name>
<read_first>
- app/src/features/training/MapExplorerPage.tsx
- app/src/lib/session.ts
</read_first>
<action>
In `MapExplorerPage.tsx`, replace all hardcoded `64` literals with `TOTAL_REGIONS` imported from session.ts or `states.length`:
- Line 24: MILESTONES array `{ count: 64, ... }` → `{ count: TOTAL_REGIONS, ... }`
- Line 31: MILESTONE_ICON_COMPONENTS `64: Trophy` → use dynamic key or TOTAL_REGIONS
- Line 386: `{clicked.length} / {states.length || 64}` → `{clicked.length} / {states.length || TOTAL_REGIONS}`
- Line 398: `total={states.length || 64}` → `total={states.length || TOTAL_REGIONS}`
- Line 556: Progress text → same replacement
- Line 561: Progress bar width calculation → same replacement
- Line 758: Tour text `'Explore all 64 regions...'` → `'Explore all ${TOTAL_REGIONS} regions...'`

Import `TOTAL_REGIONS` from `../../lib/session` if not already imported.
</action>
<acceptance_criteria>
- No hardcoded `64` literals remain in MapExplorerPage.tsx
- TOTAL_REGIONS is imported from session.ts
- TypeScript compiles without errors
</acceptance_criteria>
</task>

<task id="4" type="execute">
<name>Replace hardcoded 64 in TrainingCompletePage</name>
<read_first>
- app/src/features/training/TrainingCompletePage.tsx
- app/src/lib/session.ts
</read_first>
<action>
In `TrainingCompletePage.tsx`:
- Line 125: Copy text `'You explored all 64 states...'` → `'You explored all ${TOTAL_REGIONS} states...'`
- Line 135: Badge `{ count: 64, label: 'Completionist' }` → `{ count: TOTAL_REGIONS, label: 'Completionist' }`

Import `TOTAL_REGIONS` from `../../lib/session` if not already imported.
</action>
<acceptance_criteria>
- No hardcoded `64` literals remain in TrainingCompletePage.tsx
- Display text shows "63" at runtime
</acceptance_criteria>
</task>

<task id="5" type="execute">
<name>Replace hardcoded 64 in ExpeditionReport</name>
<read_first>
- app/src/features/results/ExpeditionReport.tsx
- app/src/lib/session.ts
</read_first>
<action>
In `ExpeditionReport.tsx`:
- Line 52: Share text `Regions Mastered: ${masteredCount}/64` → `Regions Mastered: ${masteredCount}/${TOTAL_REGIONS}`
- Line 112: Report value `value: '${masteredCount}/64'` → `value: '${masteredCount}/${TOTAL_REGIONS}'`

Import `TOTAL_REGIONS` from `../../lib/session` if not already imported.
</action>
<acceptance_criteria>
- No hardcoded `64` literals remain in ExpeditionReport.tsx
- Share text and report show correct denominator
</acceptance_criteria>
</task>

<task id="6" type="execute">
<name>Update session tests</name>
<read_first>
- app/src/__tests__/session.test.ts
- app/src/lib/session.ts
</read_first>
<action>
In `session.test.ts`:
- Line 178: Test description `'completes when 64 regions'` → `'completes when 63 regions'`
- Line 180: `Array.from({ length: 64 })` → `Array.from({ length: 63 })`

Import `TOTAL_REGIONS` from `../lib/session` and use it in test assertions instead of hardcoded values.
</action>
<acceptance_criteria>
- Session tests pass with 63 regions
- Test descriptions reflect correct count
- No hardcoded `64` in session.test.ts related to region count
</acceptance_criteria>
</task>

</tasks>

<verification>
- Click exactly 63 non-DC regions on MapExplorer → training complete screen should appear
- Clicking DC should not increment the counter
- Counter should show "X / 63" not "X / 64"
- TrainingCompletePage shows "You explored all 63 states"
- ExpeditionReport shows "Regions Mastered: X/63"
- All session tests pass
</verification>

<must_haves>
- Training completes at exactly 63 regions
- DC excluded from click tracking
- All hardcoded 64 literals replaced with TOTAL_REGIONS or states.length
- Tests updated and passing
</must_haves>
